import React from "react";

import { Container } from "./styles";

const Loading = () => {
  return (
    <Container>
      <span className="loader"></span>
    </Container>
  );
};

export default Loading;
